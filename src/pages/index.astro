---
import '../styles/global.css';
import curriculum from '../data/curriculum.json';
import Icon from '../components/Icon.astro';
import { mapTechName } from '../components/icons/icon-types';
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{curriculum.personal.nombre} - Curriculum Vitae</title>
    <meta name="description" content="Curriculum Vitae de {curriculum.personal.nombre}" />
</head>
<body>
    <div class="container">
        <header>
            <h1>{curriculum.personal.nombre}</h1>
            <p>Desarrollador Web & Formador</p>
            <div class="social-icons">
                <a href={curriculum.redes.github} target="_blank">
                    <Icon name="github" size="md" />
                </a>
                <a href={curriculum.redes.linkedin} target="_blank">
                    <Icon name="linkedin" size="md" />
                </a>
            </div>
        </header>

        <main>
            <section id="formacion">
                <h2>Formación Académica</h2>
                {curriculum.formacion.map((item) => (
                    <div class="item">
                        <h3>{item.estudios}</h3>
                        <p><strong>{item.institucion}</strong> - {item.fecha}</p>
                        <p>{item.observaciones}</p>
                    </div>
                ))}
            </section>

            <section id="experiencia">
                <h2>Experiencia Profesional</h2>
                
                <h3>Formador Ocupacional</h3>
                {curriculum.experiencia.formadorOcupacional.map((item) => (
                    <div class="item">
                        <h4>{item.cargo}</h4>
                        <p><strong>{item.empresa}</strong> - {item.fecha}</p>
                        <p>{item.descripcion}</p>
                        <p><em>{item.horas} horas | {item.observaciones}</em></p>
                    </div>
                ))}

                <h3>Profesor Universitario</h3>
                {curriculum.experiencia.profesorUniversitario.map((item) => (
                    <div class="item">
                        <h4>{item.cargo}</h4>
                        <p><strong>{item.empresa}</strong> - {item.fecha}</p>
                        <p>{item.descripcion}</p>
                        <p><em>{item.duracion} | {item.observaciones}</em></p>
                    </div>
                ))}

                <h3>Desarrollo Web</h3>
                {curriculum.experiencia.desarrolloWeb.map((item) => (
                    <div class="item">
                        <h4>{item.cargo}</h4>
                        <p><strong>{item.empresa}</strong> - {item.fecha}</p>
                        <p>{item.descripcion}</p>
                        <p><em>{item.observaciones}</em></p>
                    </div>
                ))}

                <h3>Diseño Gráfico</h3>
                {curriculum.experiencia.disenoGrafico.map((item) => (
                    <div class="item">
                        <h4>{item.cargo}</h4>
                        <p><strong>{item.empresa}</strong> - {item.fecha}</p>
                        <p>{item.descripcion}</p>
                        <p><em>{item.observaciones}</em></p>
                    </div>
                ))}

                <h3>Producción Audiovisual</h3>
                {curriculum.experiencia.audiovisual.map((item) => (
                    <div class="item">
                        <h4>{item.cargo}</h4>
                        <p><strong>{item.empresa}</strong> - {item.fecha}</p>
                        <p>{item.descripcion}</p>
                        <p><em>{item.observaciones}</em></p>
                    </div>
                ))}
            </section>

            <section id="tecnologias">
                <h2>Tecnologías y Herramientas</h2>
                <div class="tech-grid">
                    <!-- Lenguajes -->
                    <div class="tech-category">
                        <h3>Lenguajes</h3>
                        <div class="tech-items">
                            {curriculum.tecnologias.desarrolloWeb.lenguajes.map((tech) => (
                                <div class="tech-item">
                                    <Icon name={mapTechName(tech)} size="lg" />
                                    <span>{tech}</span>
                                </div>
                            ))}
                        </div>
                    </div>

                    <!-- Frameworks -->
                    <div class="tech-category">
                        <h3>Frameworks</h3>
                        <div class="tech-items">
                            {curriculum.tecnologias.desarrolloWeb.frameworks.map((tech) => (
                                <div class="tech-item">
                                    <Icon name={mapTechName(tech)} size="lg" />
                                    <span>{tech}</span>
                                </div>
                            ))}
                        </div>
                    </div>

                    <!-- Herramientas -->
                    <div class="tech-category">
                        <h3>Herramientas</h3>
                        <div class="tech-items">
                            {curriculum.tecnologias.desarrolloWeb.herramientas.map((tech) => (
                                <div class="tech-item">
                                    <Icon name={mapTechName(tech)} size="lg" />
                                    <span>{tech}</span>
                                </div>
                            ))}
                        </div>
                    </div>

                    <!-- Bases de Datos -->
                    <div class="tech-category">
                        <h3>Bases de Datos</h3>
                        <div class="tech-items">
                            {curriculum.tecnologias.desarrolloWeb.baseDatos.map((tech) => (
                                <div class="tech-item">
                                    <Icon name={mapTechName(tech)} size="lg" />
                                    <span>{tech}</span>
                                </div>
                            ))}
                        </div>
                    </div>

                    <!-- Expresión Audiovisual -->
                    <div class="tech-category">
                        <h3>Expresión Audiovisual</h3>
                        <div class="tech-items">
                            {[
                                ...curriculum.tecnologias.expresionAudiovisual.imagenRaster,
                                ...curriculum.tecnologias.expresionAudiovisual.imagenVectorial,
                                ...curriculum.tecnologias.expresionAudiovisual.video
                            ].map((tech) => (
                                <div class="tech-item">
                                    <Icon name={mapTechName(tech)} size="lg" />
                                    <span>{tech}</span>
                                </div>
                            ))}
                        </div>
                    </div>

                    <!-- Despliegue & Testing -->
                    <div class="tech-category">
                        <h3>Despliegue & Testing</h3>
                        <div class="tech-items">
                            {[
                                ...curriculum.tecnologias.desarrolloWeb.despliegue,
                                ...curriculum.tecnologias.desarrolloWeb.testing
                            ].map((tech) => (
                                <div class="tech-item">
                                    <Icon name={mapTechName(tech)} size="lg" />
                                    <span>{tech}</span>
                                </div>
                            ))}
                        </div>
                    </div>
                </div>
            </section>

            <section id="contacto">
                <h2>Contacto</h2>
                <div class="contact-info">
                    <p><strong>Ubicación:</strong> {curriculum.personal.contacto.calle}, {curriculum.personal.contacto.ciudad}</p>
                    <p><strong>Teléfono:</strong> {curriculum.personal.contacto.movil}</p>
                    <p><strong>Email:</strong> <a href="mailto:{curriculum.personal.contacto.email}">{curriculum.personal.contacto.email}</a></p>
                    <p><strong>Fecha de nacimiento:</strong> {curriculum.personal.datosNacimiento.fechaNacimiento} | {curriculum.personal.datosNacimiento.lugarNacimiento}</p>
                </div>
            </section>
        </main>

        <footer>
            <p>&copy; 2026 {curriculum.personal.nombre}. Todos los derechos reservados.</p>
        </footer>
    </div>
</body>
</html>